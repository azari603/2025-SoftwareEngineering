정도희
#25 프로필 수정
<img src="./25.png">
  사용자가 프로필 설정 화면에서 닉네임이나 소개를 바꾸고 저장을 누르면, 시스템은 먼저 DB에서 현재 프로필을 읽어 와서 새 값이 규칙에 맞는지 점검한다. 금지어, 길이, 포맷이 어긋나면 저장을 멈추고 화면에 입력 형식 오류를 보여 준다. 문제가 없으면 바뀐 내용을 프로필에 반영해 DB에 기록하고, 성공으로 응답하면서 화면에는 갱신된 값이 그대로 반영된다.
  프로필 이미지를 바꿀 때는 사용자가 올린 파일의 유형, 크기, 해상도를 먼저 검사한다. 규격을 벗어나면 즉시 이미지 형식 오류를 알려 주고 업로드를 중단한다. 검사를 통과하면 이미지 보관소에 파일을 업로드하고 공개 주소를 만든 뒤, 그 주소를 프로필에 연결해 DB에 저장한다. 저장이 끝나면 새 이미지 주소가 담긴 성공 응답이 돌아와 화면에 바로 교체된 이미지가 보인다.
  배경 이미지를 바꿀 때도 흐름은 동일하다. 파일 검사를 거쳐 문제가 없으면 업로드와 주소 발급을 진행하고, 해당 주소를 프로필의 배경으로 연결해 저장한다. 이후 성공 응답과 함께 화면에서 배경이 즉시 갱신된다.
  DB나 저장소 접근 중 오류가 나면 해당 단계에서 중단하고 서버 오류로 응답하며, 화면은 실패 안내를 보여 주고 기존 상태를 유지한다.

#26 이달의 목표 설정
<img src="./26.png">
  사용자가 ‘나의 기록 → 독서 통계’에서 목표 설정 버튼을 눌러 값을 입력하고 완료를 누르면, 시스템은 우선 입력이 정수인지와 허용 범위인지(0 이상) 확인한다. 값이 0이하면 이번 달 목표를 해제하는 의미로 해석하고, 양수면 해당 값으로 목표를 잡는다. 검증을 통과하면 시스템은 DB에서 사용자의 프로필을 읽어 온 뒤, 목표 필드를 새 값(또는 해제)으로 바꾸고 저장한다. 저장이 끝나면 화면에는 별도 본문 없이 성공으로 응답하고(내용 없이 완료), 통계 위젯의 목표 및 달성률이 즉시 갱신된다.
  입력이 정수가 아니거나 허용 범위를 벗어나면 저장을 진행하지 않고 팝업에 입력 오류 안내를 띄운다. DB 접근 중 문제가 생기면 저장을 중단하고 서버 오류를 알리며, 화면의 목표 값은 이전 상태를 유지한다.

#27 통계 기록 보기
<img src="./27.png">
  사용자가 ‘나의 기록’에서 ‘독서 통계’를 열면, 화면은 먼저 개요·별점 히스토그램·타임라인·자주 읽은 저자·카테고리 분포·이달의 목표/진척을 순차(또는 병렬)로 요청한다. 시스템은 로그인 사용자를 기준으로 DB에서 본인이 작성한 서평과 완독 상태 기록을 읽어 온다. 개요와 카테고리 분포, 자주 읽은 저자는 서평 목록을 집계해 총 읽은 권수, 평균 별점, 별점 분포, 저자 상위 랭킹, 카테고리 비율을 계산해 돌려준다. 별점 히스토그램은 서평들의 평점을 그대로 세어 막대 데이터로 만든다. 타임라인은 완독 상태 기록을 기간별로 묶어 월(또는 연) 단위 카운트를 만들어 전달한다. 이달의 목표/진척은 사용자의 프로필에서 목표 값을 확인하고, 이번 달 완독 수를 더해 달성률을 계산해 보여준다. 어떤 단계에서든 DB 접근이나 네트워크 문제가 발생하면 해당 카드 대신 오류 메시지를 띄우고, 나머지 카드들은 가능한 범위에서 정상 데이터를 계속 보여준다.


#28 자주 읽은 저자 도서 목록 보기
<img src="./28.png">
  사용자가 ‘나의 기록 → 독서 통계’에서 자주 읽은 저자 목록의 펼치기 버튼을 누르면, 화면은 선택된 저자를 기준으로 내가 완독한 기록만 모으기 위해 먼저 독서 상태 저장소에서 해당 사용자의 완독(Completed) 기록을 읽어 온다. 이때 완독 목록 안에서 저자 기준으로 묶어 빈도 상위를 계산하고, 사용자가 펼친 저자에 대해서는 그 저자의 책들 중 내가 완독한 항목들만 추려 낸다. 추려진 각 책에 대해서는 도서 저장소에서 표지 이미지와 제목 등 메타 정보를 가져와 카드 목록을 만든다. 이렇게 합쳐진 결과가 컨트롤러로 전달되고, 컨트롤러는 확장된 컨테이너에 표지와 제목이 나열된 리스트를 그려 준다.
  완독 기록 안에 해당 저자의 책이 하나도 없으면, 확장 컨테이너는 비어 있는 상태로 표시되고 별도 경고는 띄우지 않는다. DB 접근이나 네트워크 오류가 발생하면 목록 생성을 중단하고 오류 안내만 보여 주며, 기존 화면 상태는 유지된다.

#29 알림 확인
<img src="./29.png">
  알림 아이콘을 누르면 화면은 먼저 내 알림을 시간 내림차순으로 가져와 목록을 그린다. 동시에 읽지 않은 알림의 개수를 세어 배지에 표시한다. 목록이 열려 있는 동안 내가 특정 알림 카드를 눌러 상세로 이동하거나 확인 동작을 하면, 그 알림을 읽음으로 바꿔 다시 저장한다. 필요하면 카드의 메뉴에서 삭제를 선택해 해당 알림을 지울 수 있고, 목록은 즉시 갱신된다.
  DB 조회나 저장 과정에서 문제가 생기면 목록·배지 대신 오류 안내가 표시되고, 읽음 처리나 삭제는 수행되지 않는다.

#30 책 정보 불러오기
<img src="./30.png">
  사용자가 제목이나 저자 키워드를 넣어 책을 찾으면, 시스템은 먼저 내부 DB에서 해당 키워드에 맞는 책 카드들을 검색한다. 이미 저장된 결과가 있으면 그대로 목록을 만들어 돌려준다. 저장된 결과가 하나도 없으면, 시스템은 외부 도서 API에 같은 키워드로 검색을 요청하고, 받아온 책 메타를 내부 형식으로 변환해 DB에 차례로 저장한다. 인입이 끝나면 방금 저장된 데이터 기준으로 다시 한 번 DB를 조회해 책 카드 목록을 구성해준다.
  외부 API 호출이 실패하면 목록 생성은 중단되고 “외부 API 오류”로 응답한다. 호출은 성공했지만 결과가 없으면, 저장 없이 빈 목록을 그대로 반환한다.
